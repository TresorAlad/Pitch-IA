# Production Vercel (recommandé) : ne pas définir VITE_API_URL.
# Configurer le proxy dans frontend/vercel.json → EC2 :8088

# Uniquement si l'API est en HTTPS (Caddy + domaine) :
# VITE_API_URL=https://api.votre-domaine.com
